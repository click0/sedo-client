# Runtime dependency only (what `pip install .` pulls in).
# Optional extras live in pyproject.toml:
#   pip install .[pkcs11]   # PyKCS11 — pkcs11 / virtual backends
#   pip install .[test]     # pytest
#   pip install .[all]
# CI (tests.yml) installs `requests pytest` directly; PyKCS11 is never
# required — the opensc and iit_agent backends work without it.
requests>=2.31
